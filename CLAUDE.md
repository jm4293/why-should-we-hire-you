# Why Do We Hire You? — 프로젝트 기획서

> **"왜 당신을 채용해야 하나요?"**
> AI 면접관이 이력서와 포트폴리오를 분석해 서류 합격 가능성을 높여주는 서비스

---

## 1. 프로젝트 개요

### 서비스 목적

- 10년 경력 인사팀 / 면접관 입장에서 지원자의 이력서, 포트폴리오, 경력기술서를 분석
- 해당 회사 채용 공고 기준으로 적합성 판단 및 피드백 제공
- 이력서/포트폴리오 수정 제안 및 자소서 초안 작성
- 여러 AI를 각각의 면접관 페르소나로 활용해 다양한 시각의 피드백 제공

### 핵심 원칙

- **1 File = 1 Component**: 하나의 파일에는 무조건 하나의 컴포넌트만 작성
- **글로벌 모달 관리**: 모달/Dialog는 인라인으로 렌더링하지 않고, Zustand 등 전역 상태를 통해 독립된 컴포넌트로 관리
- 강조된 단어, 오바스러운 단어, 감탄사 **지양** → 자연스러운 말투
- 자소서 소제목 **필수** (소제목만 봐도 내용과 흥미 유발)
- 자소서는 하나의 질문을 여러 맥락으로 나눠서 작성
- 출력 언어: **한국어 고정**

---

## 2. 기술 스택

```
프레임워크        Next.js 14+ (App Router)
스타일링          Tailwind CSS + shadcn/ui
아이콘            Lucide React
애니메이션        GSAP + ScrollTrigger (랜딩 페이지)
AI 통합           Vercel AI SDK (OpenAI / Claude / Gemini 통합 관리)
AI SDK            openai / @anthropic-ai/sdk / @google/generative-ai
스트리밍          Edge Runtime (Vercel 무료 플랜, 최대 30초)
PDF 파싱          pdfjs-dist (클라이언트)
PDF 생성          html2pdf.js (클라이언트, 무료)
로컬 저장         idb (IndexedDB wrapper)
암호화            crypto-js (API 키 암호화)
크롤링            cheerio + fetch (API Route)
상태 관리         Zustand
배포              Vercel (무료 플랜)
```

### Vercel 무료 플랜 주의사항

- API Route 실행 시간 제한: **10초** → AI 스트리밍은 Edge Runtime 사용 (최대 30초)
- Puppeteer 등 무거운 크롤러 사용 불가 → `cheerio + fetch`로 경량 크롤링

---

## 3. 프로젝트 구조

```
src/
├── app/
│   ├── page.tsx                  # 랜딩 페이지
│   ├── analyze/
│   │   └── page.tsx              # 메인 앱 (분석 입력 + 결과)
│   ├── settings/
│   │   └── page.tsx              # API 키 설정 페이지
│   └── api/
│       ├── crawl/route.ts        # URL 크롤링 (Edge Runtime)
│       └── analyze/route.ts      # AI 스트리밍 (Edge Runtime)
├── components/
│   ├── common/                   # 공통 사용 컴포넌트 (Guard 등)
│   ├── modals/                   # 전역 모달 컴포넌트 (GlobalModalProvider 등)
│   ├── landing/                  # 랜딩 페이지 섹션 컴포넌트
│   ├── analyze/                  # 스텝별 입력 컴포넌트
│   ├── result/                   # 결과 화면 컴포넌트
│   ├── history/                  # 히스토리 사이드바
│   └── settings/                 # 설정 컴포넌트
├── lib/
│   ├── ai/                       # Vercel AI SDK 래퍼
│   ├── pdf/                      # PDF 파싱/생성
│   ├── storage/                  # IndexedDB 관리
│   ├── crypto/                   # API 키 암호화
│   └── crawl/                    # 크롤링 유틸
├── store/                        # Zustand 스토어 (analysis, modal 등)
├── types/                        # TypeScript 타입 정의
└── hooks/                        # 커스텀 훅
```

---

## 4. 페이지 구성

### 4-1. 랜딩 페이지 (`/`)

- **모바일 대응**: O (랜딩만)
- **테마**: 라이트 모드 전용
- **애니메이션**: GSAP + ScrollTrigger (Apple 공식 홈페이지 스타일)
- **폰트**: Pretendard (한국어 지원 최적)

**섹션 구성:**

```
섹션 1. Hero        — "Why do we hire you?" 타이틀, 서브카피, 시작하기 버튼
섹션 2. 문제 제기   — "서류 탈락 이유를 모르고 계신가요?" 공감 문구
섹션 3. 기능 소개   — 3단계 플로우 (입력 → AI 분석 → 결과 확인)
섹션 4. 면접관 소개 — 페르소나 시각화 (직무 전문가 / HR / 팀장 예시)
섹션 5. 주의 안내   — API 키 필요, 개인 PC 권장, 비용 안내
섹션 6. CTA        — 시작하기 버튼
```

### 4-2. 메인 앱 (`/analyze`)

- **모바일 접근 시**: 데스크탑 접근 안내 UI 표시
- **API 키 없을 시**: 모달로 안내 → 설정 페이지 이동

**레이아웃:**

```
┌──────────────────────────────────────────────────┐
│  [로고]                          [새 분석] [설정] │  ← 상단 네비
├────────┬─────────────────────────────────────────┤
│히스토리 │                                         │
│사이드바 │         메인 콘텐츠 영역                  │
│(YouTube│   (입력 스텝 or 결과 화면)                │
│스타일) │                                         │
└────────┴─────────────────────────────────────────┘
```

### 4-3. 설정 페이지 (`/settings`)

- 랜딩에서 직접 접근 불가 (메인 앱 상단 네비에서만 접근)

---

## 5. 히스토리 사이드바

- **위치**: 좌측 (YouTube 스타일)
- **기본값**: 펼쳐진 상태
- **펼침**: 아이콘 + 회사명 / 직무명 / 날짜 / 사용된 AI 목록
- **접힘**: 아이콘만 표시
- **아이콘**: 회사 파비콘 (없으면 기본 아이콘)
- **삭제**: 개별 삭제 + 전체 삭제
- **용량 초과 시**: 오래된 항목부터 자동 삭제 안내
- **저장 시점**: 사용자가 직접 저장 버튼 클릭 시

---

## 6. 입력 플로우 (스텝별 Wizard)

```
Step 1. 회사 정보
  - 회사 소개 URL (크롤링, 실패 시 에러 메시지만)
  - 채용 공고 URL (크롤링, 실패 시 에러 메시지만)

Step 2. 내 서류
  - 이력서 PDF (필수, 단독 input)
  - 추가 서류 PDF (선택, 여러 개 가능 — 포트폴리오, 경력기술서 등)

Step 3. 자소서 문항
  - 문항 추가 / 삭제
  - 문항별 최대 글자 수 설정

Step 4. 페르소나 설정
  - 등록된 AI API 키 수만큼 면접관 생성
  - 각 면접관마다 직무명 + 역할 설명 직접 작성
  - 예시 제공 (처음 사용자 안내용)
  - 설정 저장 → 다음 분석에 재사용 가능
  - API 키 미등록 시 설정 페이지 이동 안내

Step 5. 최종 확인
  - 입력 내용 요약
  - API 비용 발생 안내 (확인 모달)
  - 분석 시작 버튼
```

### 임시저장

- 스텝 이동 시 **자동으로 IndexedDB에 저장**
- 재진입 시 "이전 작업을 이어서 하시겠습니까?" 복원 모달
- 새로고침 / 페이지 이탈 시 **경고 알림** ("입력 중인 데이터가 삭제됩니다")
- 분석 시작 또는 사용자 초기화 시 임시저장 삭제
- PDF 파일 자체는 저장하지 않음 (파일명 + 파싱 텍스트만 저장, 재업로드 안내)

### 스텝 간 이동

- 이전 스텝으로 돌아가도 **입력값 유지**
- 브라우저 뒤로가기 처리 포함

---

## 7. 결과 화면

### 레이아웃

```
┌─────────────────────────────────────────────────────┐
│  [면접관 A 🔄] [면접관 B ✓] [면접관 C 🔄]           │  ← 탭 (스피너→체크)
├─────────────────────────┬───────────────────────────┤
│   원본 PDF 뷰어 (50%)    │   AI 분석 결과 (50%)       │
│                         │                           │
│  [이력서] [추가서류1]    │  ▌ 스트리밍 중...           │
│  ← 탭으로 전환           │                           │
│                         │  1. 종합 평가              │
│  [독립 스크롤]           │  2. 적합한 이유             │
│                         │  3. 보완점                 │
│                         │  4. 수정 제안              │
│                         │  5. 자소서 초안             │
│                         │     [문항1] [문항2] ...    │
│                         │     ← 탭으로 전환           │
│                         │  [독립 스크롤]              │
├─────────────────────────┴───────────────────────────┤
│  [저장]  [PDF 다운로드 ▼ — 전체 / 면접관 선택]        │
└─────────────────────────────────────────────────────┘
```

### 스트리밍

- 면접관 A, B, C **동시 병렬 스트리밍** (`Promise.all`)
- 각 면접관 탭 독립적으로 스트리밍
- 탭에 로딩 스피너 → 완료 시 체크 아이콘으로 변경
- 커서(▌) 블링킹 효과로 작성 중 표시
- 섹션별 스켈레톤이 순차적으로 채워지는 방식

### 결과 섹션 (고정 구조)

```
1. 종합 평가       — 카드 형태 강조 표시
2. 적합한 이유     — 텍스트
3. 보완점          — 텍스트
4. 이력서 수정 제안 — 원문 → 수정안 비교 형태
5. 자소서 초안     — 문항별 탭
```

### 오류 처리

- 특정 면접관 실패 시: 해당 탭에 에러 표시 + 재시도 버튼
- 전체 실패 시: 재시도 또는 처음으로 돌아가기

### PDF 다운로드

- **생성 방식**: html2pdf.js (클라이언트 사이드)
- **범위 선택**: 전체 다운로드 또는 특정 면접관 선택
- **포함 내용**: 원본 이력서 내용 + AI 분석 결과 나란히
- **레이아웃**: 원본 / 수정 제안 나란히 표시

---

## 8. 면접관 페르소나

### 개념

같은 이력서를 **다른 기준**으로 평가해 다양한 시각의 피드백 제공

### 예시

| 면접관   | 역할        | 평가 기준                                    |
| -------- | ----------- | -------------------------------------------- |
| 면접관 A | 직무 전문가 | 기술 스택, 실무 경험, 요구 역량 충족 여부    |
| 면접관 B | HR 담당자   | 조직 문화 적합성, 이직 주기, 협업 경험       |
| 면접관 C | 팀장        | 성장 가능성, 자기주도 학습, 온보딩 가능 여부 |

### 설정 방식

- 사용자가 직무명 + 역할 설명 직접 작성
- 예시 텍스트 제공 (처음 사용자용)
- 설정 저장 → 다음 분석 시 재사용 가능
- 등록된 AI API 키 수에 따라 면접관 수 결정

---

## 9. 설정 페이지

### AI API 키 관리

- **지원 AI**: OpenAI / Anthropic Claude / Google Gemini
- **저장 방식**: 로컬스토리지 + crypto-js 암호화
- **표시 방식**: 마스킹 처리 (●●●●●●●●)
- **기능**:
  - API 키 등록 / 수정 / 삭제
  - 유효성 테스트 버튼 (등록 전 키 확인)
  - 각 AI 서비스별 API 키 발급 방법 가이드 + 링크

### 안내 사항

- **개인 PC 사용 권고**: "API 키는 이 기기의 브라우저에만 저장됩니다. 공용 PC에서는 사용하지 마세요."
- **비용 안내**: "분석 시 등록된 API 키로 비용이 발생합니다. 1회 분석 기준 약 $0.01~$0.10 (모델에 따라 상이)"

### 페르소나 저장

- 설정 페이지 또는 Step 4에서 저장한 페르소나 재사용 가능

---

## 10. 자소서 작성 원칙

- 하나의 질문을 **여러 맥락**으로 나눠서 작성
- **소제목 필수** (소제목만 봐도 내용 파악 + 흥미 유발)
- 강조된 단어, 오바스러운 단어, 감탄사 **지양**
- 자연스러운 말투 유지
- 최대 글자 수 제한 내에서 작성
- 각 면접관 페르소나 관점에서 별도로 작성

---

## 11. 데이터 저장 전략

### IndexedDB (idb 라이브러리)

```
임시저장 (draft)
  - 입력 중인 스텝 데이터 (PDF 파일 자체 제외)
  - PDF 파일명 + 파싱된 텍스트

히스토리 (history)
  - 날짜, 회사명, 직무명, 사용된 AI 목록
  - 분석 결과 전체 텍스트
  - 사용자가 저장 버튼 클릭 시 저장

페르소나 (personas)
  - 저장된 면접관 페르소나 목록
  - 재사용 가능
```

### 로컬스토리지 (crypto-js 암호화)

```
API 키
  - OpenAI API Key
  - Anthropic API Key
  - Google Gemini API Key
```

### 용량 관리

- IndexedDB 용량 초과 시 오래된 히스토리부터 자동 삭제 안내
- 히스토리 개별 삭제 + 전체 삭제 기능 제공

---

## 12. 모바일 대응

| 페이지               | 모바일                    |
| -------------------- | ------------------------- |
| 랜딩 페이지          | O (대응)                  |
| 메인 앱 (`/analyze`) | X (데스크탑 접근 안내 UI) |
| 설정 페이지          | X (데스크탑 접근 안내 UI) |

---

## 13. 개발 우선순위

### Phase 1 — 기반 구축

- [ ] Next.js 프로젝트 초기 설정
- [ ] Tailwind CSS + shadcn/ui + Lucide 설정
- [ ] Zustand 스토어 구성
- [ ] IndexedDB / 로컬스토리지 유틸 구성

### Phase 2 — 랜딩 페이지

- [ ] GSAP + ScrollTrigger 설정
- [ ] 6개 섹션 구현
- [ ] 반응형 (모바일 대응)

### Phase 3 — 설정 페이지

- [ ] API 키 등록 / 수정 / 삭제
- [ ] 유효성 테스트
- [ ] 발급 가이드

### Phase 4 — 입력 Wizard

- [ ] 5단계 스텝 구현
- [ ] 임시저장 (IndexedDB)
- [ ] PDF 파싱 (pdfjs-dist)
- [ ] URL 크롤링 (API Route)

### Phase 5 — AI 분석 & 결과

- [ ] Vercel AI SDK 스트리밍 설정
- [ ] 병렬 스트리밍 (Promise.all)
- [ ] 결과 화면 (PDF 뷰어 + 탭 구조)
- [ ] PDF 다운로드 (html2pdf.js)

### Phase 6 — 히스토리 & 마무리

- [ ] 히스토리 사이드바
- [ ] 에러 처리 전반
- [ ] 성능 최적화

---

## 14. 참고 스킬

개발 시 아래 스킬 문서를 기준으로 코드 작성:

- `.claude/skills/vercel-react-best-practices/` — React/Next.js 성능 최적화 (57개 규칙)
- `.claude/skills/vercel-composition-patterns/` — React 컴포넌트 구성 패턴 (8개 규칙)
- `.claude/skills/web-design-guidelines/` — UI/UX 접근성 및 디자인 가이드라인
